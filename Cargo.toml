[package]
name = "retro-tts"
version = "0.1.0"
edition = "2021"

#[features]
default = ["candle"]
candle = ["dep:candle", "candle-nn"]

[dependencies]
anyhow = "1.0.65"
candle-core = { version = "0.3.0", optional = true }
candle-nn = { version = "0.3.0", optional = true }
deunicode = "1.3.2"
hound = "3.5.0"
once_cell = "1.15.0"
regex = "1.6.0"
